Булевый тип bool - это логический, специальный целочисленный тип, используемый для представления истинности или ложности. Переменная такого типа будет занимать только один байт.

bool имеет три значения:
	&& - и
	|| - или 
	! - не

Переменная типа bool может принимать только значения true и false.
