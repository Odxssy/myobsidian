Print - выводит данные в одну строку если между ними нет типа string, в таком случае она "склеит" переменные между собой.

Println - всегда выводит данные через пробел.

Пример:

var a = 10
var b = 20
fmt.Print(a , b) 
Вывод : 10  20

var a = 10
var b string = "Hello"
amt.Print(a) 
Вывод: 10Hello